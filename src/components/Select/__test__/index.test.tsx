import { screen, render } from '@testing-library/react'
import { Select } from '..'

describe('Text field', () => {
  it('Form input should have a label', () => {
    // render(<Select label="" options={[{}]} />)
  })
})
